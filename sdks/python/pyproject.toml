[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "akidb"
version = "0.10.0"
description = "Python SDK for AkiDB, a portable retrieval database for private AI systems"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
dependencies = [
    # Keep runtime floors aligned with the pinned, security-patched generator.
    "grpcio>=1.68.1,<2",
    "protobuf>=5.29.6,<6",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
]

[tool.setuptools]
packages = ["akidb"]
