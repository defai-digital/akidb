[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "akidb"
version = "0.10.0"
description = "Python SDK for AkiDB, a portable retrieval database for private AI systems"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
dependencies = [
    # Generated stubs (akidb_pb2*) target current grpcio/protobuf runtimes.
    "grpcio>=1.62",
    "protobuf>=5.29",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
]

[tool.setuptools]
packages = ["akidb"]
