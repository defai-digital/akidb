---
# Compatibility shim: the dual-mode service plane lives in service_network.
# Existing playbooks or inventories that still name the wireguard role keep working.
- name: Delegate to dual-mode service_network role
  ansible.builtin.include_role:
    name: service_network
